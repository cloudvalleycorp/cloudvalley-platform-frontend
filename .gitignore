# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Variables de entorno: nunca se commitean (incidente 2026-08-29, ver
# docs/security-env-vars.md). Solo .env.example queda versionado.
.env
.env.*
!.env.example

# Playwright MCP: storageState.json contiene la cookie de sesión real
# (HttpOnly, viva) capturada por scripts/playwright/login.mjs — nunca se commitea.
playwright/.auth/
playwright/.output/
# Archivos de prueba (ej. .xlsx generados para probar el flujo de Excel) —
# scratch local, nunca deberían terminar versionados.
playwright/fixtures/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.docs/*